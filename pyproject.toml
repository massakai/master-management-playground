[project]
name = "master-management-playground"
version = "0.1.0"
description = "Playground for a product category master management MVP"
requires-python = ">=3.11"

[dependency-groups]
dev = [
  "Jinja2>=3.1.6",
  "PyYAML>=6.0.3",
]
